module(pattern;;)
---*
accesFichierPattern = ouverture fichier "pattern.csv" en ecriture
accesFichierTrainSet = ouverture fichier "trainset.csv" en lecture

// ajout des noms des colonnes au fichier "pattern.csv"

index = 0;
accesFichierPattern ajout "Mouvement"
while (index < 600)
  accesFichierPattern ajout "Vacc"
  index++
endwhile

accesFichierPattern ajout FIN_DE_LIGNE

accelerations = VECTOR
acceleartions.clean() 

while ((accesFichierTrainSet) obtenir LIGNE != false)
  (LIGNE) obtenir mouvement
  mouvementActuelle = mouvement

  nbLignesBlock = 0
  while (mouvement == mouvementObtenue AND (accesFichierTrainSet) obtenir LIGNE != false)
    (LIGNE) obtenir mouvement, person_id, gender // objectif : se déplacer dans la ligne

    acc_index = 0
    while ((LIGNE) obtenir acceleration != false)
      if (acc_index < accelerations.size())
        accelerations[acc_index] += acceleration 
        acc_index++ 
      else
        accelerations.push_back(acceleration);
      endif
      acc_index++
    endwhile
    nbBlockLigne++
  endwhile
  acc_index = 0
  while (acc_index < accelerations.size())
    accesFichierPattern ajout accelerations[acc_index] / nbLignesBlock
    acc_index++
  endwhile
    accesFichierPattern ajout FIN_DE_LIGNE
endwhile
fermeture fichier accesFichierPattern
fermeture fichier accesFichierTrainSet
------